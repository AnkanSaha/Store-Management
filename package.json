{
  "name": "store-management-backend",
  "version": "1.0.2",
  "description": "Manage Your Inventory with this website",
  "main": "store.ts",
  "scripts": {
    "dev": "tsc && cp package.json Build/ && cp package-lock.json Build/ && nodemon Build/store.js --watch Build/",
    "build": "tsc && cp package.json Build/ && cp package-lock.json Build/ && npm install --production"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AnkanSaha"
  },
  "keywords": [
    "store-management"
  ],
  "type": "module",
  "author": "Ankan Saha",
  "license": "EPL-1.0",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
