{
  "name": "store-management-backend",
  "version": "1.0.2",
  "description": "Manage Your Inventory with this website",
  "main": "store.ts",
  "scripts": {
    "dev": "tsc && cp package.json Build/ && cp package-lock.json Build/ && nodemon Build/store.js --watch",
    "build": "tsc && cp package.json Build/ && cp package-lock.json Build/ && npm install --production",
    "start": "pm2 start store.js --name store-management-backend --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AnkanSaha"
  },
  "keywords": [
    "store-management"
  ],
  "author": "Ankan Saha",
  "license": "EPL-1.0",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.1.0",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
